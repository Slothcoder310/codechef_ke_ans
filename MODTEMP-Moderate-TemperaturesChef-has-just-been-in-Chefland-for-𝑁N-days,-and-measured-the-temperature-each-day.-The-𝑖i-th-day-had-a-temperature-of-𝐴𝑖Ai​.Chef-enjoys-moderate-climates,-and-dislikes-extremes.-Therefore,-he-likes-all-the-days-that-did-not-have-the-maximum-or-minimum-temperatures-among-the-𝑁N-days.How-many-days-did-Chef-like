# cook your dish here

test = int(input())

for _ in range(test):
    num = int(input())
    lst = list(map(int, input().split()))
    
    least, highest = min(lst), max(lst)
    count = 0
    
    for i in lst:
        if i != least and i != highest:
            count += 1
            
    print(count)